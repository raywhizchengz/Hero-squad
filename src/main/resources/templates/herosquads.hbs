{{#partial "content"}}
    <h1>All Squads</h1>
    <div class="row">
        <ul >
            {{#each squads}}
                <li><a href="/squadId"{{id}}">{{name}}</a></li>
            {{/each}}
        </ul>

    </div>

{{/partial}}
{{>layout.hbs}}